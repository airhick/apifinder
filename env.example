# No GitHub token needed! The tool uses web scraping to bypass API limits.

# Repos per second to crawl (default: 100)
# Set to 100 for maximum speed. The program will handle rate limits automatically.
REPOS_PER_SECOND=100.0

# Maximum number of repositories to crawl (default: 10000)
MAX_REPOS=10000

